import { PlaceholderPage } from './PlaceholderPage'

export default function PartnershipsPage() {
  return (
    <PlaceholderPage
      title="Startup Partnerships"
      description="Tinted hero, filter chips, startup card grid, and apply flow."
    />
  )
}
